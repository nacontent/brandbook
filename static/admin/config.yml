backend:
  name: git-gateway
  branch: main # Если в GitHub ветка master, напиши master

media_folder: "static/uploads"
public_folder: "/uploads"

collections:
  - name: "docs"
    label: "Документация"
    folder: "content/docs"
    extension: "md"
    format: "frontmatter"
    frontmatter_delimiter: "+++" # КРИТИЧНО для твоих файлов
    create: true
    fields:
      - {label: "Заголовок", name: "title", widget: "string"}
      - {label: "Текст", name: "body", widget: "markdown"}